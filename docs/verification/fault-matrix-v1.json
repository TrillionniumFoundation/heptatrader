{
  "schema": "heptatrader.fault-matrix.v1",
  "faults": [
    {"id":"F-JOURNAL-BEFORE","point":"before journal append","expected":"no venue send; typed failure"},
    {"id":"F-JOURNAL-AFTER","point":"after durable append before send","expected":"durable accepted/uncertain; restart resolves without new command"},
    {"id":"F-SEND-AFTER","point":"after send before response","expected":"uncertain + authoritative reconcile; no blind retry"},
    {"id":"F-CALLBACK-DUP","point":"duplicate callback","expected":"idempotent projection"},
    {"id":"F-CALLBACK-ORDER","point":"out-of-order callback","expected":"reject/buffer/reconcile to convergence"},
    {"id":"F-SNAPSHOT-RACE","point":"fill/reconnect during snapshot","expected":"complete same generation or reject"},
    {"id":"F-PROPOSAL-STALE","point":"expired proposal","expected":"excluded from optimizer"},
    {"id":"F-SOLVER-TIMEOUT","point":"optimizer timeout","expected":"bounded feasible fallback or no-risk plan"},
    {"id":"F-MODULE-CRASH","point":"strategy module crash","expected":"proposal expires; OMS/Execution unaffected"},
    {"id":"F-BACKPRESSURE","point":"queue full","expected":"class-specific reject/coalesce; emergency lane preserved"},
    {"id":"F-NUMERIC","point":"overflow/NaN/Inf","expected":"fail closed"},
    {"id":"F-FENCE","point":"stale epoch/lease","expected":"no venue send"}
  ]
}

diff --git a/HeptaTrade/HeptaTrader.vcxproj b/HeptaTrade/HeptaTrader.vcxproj
index de44a8d..d246b57 100644
--- a/HeptaTrade/HeptaTrader.vcxproj
+++ b/HeptaTrade/HeptaTrader.vcxproj
@@ -1,5 +1,5 @@
 锘??xml version="1.0" encoding="utf-8"?>
-<Project DefaultTargets="Build" ToolsVersion="15.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
+<Project DefaultTargets="Build" ToolsVersion="15.0" TreatAsLocalProperty="Configuration;Platform;HeptaBuildProfile;IBApiRoot;IBApiBin;HeptaEnableIbApi" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
   <ItemGroup Label="ProjectConfigurations">
     <ProjectConfiguration Include="Debug|Win32">
       <Configuration>Debug</Configuration>
@@ -69,22 +69,8 @@
     <Import Project="$(UserRootDir)\Microsoft.Cpp.$(Platform).user.props" Condition="exists('$(UserRootDir)\Microsoft.Cpp.$(Platform).user.props')" Label="LocalAppDataPlatform" />
   </ImportGroup>
   <PropertyGroup Label="UserMacros" />
-  <PropertyGroup>
-    <!-- IB API auto-discovery (override with /p:IBApiRoot=... or env IBAPI_ROOT) -->
-    <IBApiRoot Condition="'$(IBApiRoot)'=='' and '$(IBAPI_ROOT)'!=''">$(IBAPI_ROOT)</IBApiRoot>
-    <IBApiRoot Condition="'$(IBApiRoot)'=='' and Exists('$(SolutionDir)Interface\IBApi\source\CppClient\EClient.cpp')">$(SolutionDir)Interface\IBApi\source\CppClient</IBApiRoot>
-
-    <HeptaEnableIbApi Condition="'$(HeptaEnableIbApi)'=='' and Exists('$(IBApiRoot)\EClient.cpp')">true</HeptaEnableIbApi>
-    <HeptaEnableIbApi Condition="'$(HeptaEnableIbApi)'==''">false</HeptaEnableIbApi>
-    <HeptaIbPreprocessor Condition="'$(HeptaEnableIbApi)'=='true'">HEPTA_ENABLE_IBAPI;</HeptaIbPreprocessor>
-    <IBApiBin Condition="'$(IBApiBin)'=='' and '$(IBAPI_BIN)'!=''">$(IBAPI_BIN)</IBApiBin>
-    <IBApiBin Condition="'$(IBApiBin)'=='' and Exists('$(SolutionDir)Interface\IBApi\bin')">$(SolutionDir)Interface\IBApi\bin</IBApiBin>
-    <IBApiBin Condition="'$(IBApiBin)'=='' and Exists('$(IBApiRoot)\..\..\bin')">$(IBApiRoot)\..\..\bin</IBApiBin>
-
-    <HeptaIbLibDir Condition="Exists('$(SolutionDir)Interface\IBApi\lib')">$(SolutionDir)Interface\IBApi\lib</HeptaIbLibDir>
-    <HeptaIbAdditionalDependencies Condition="Exists('$(HeptaIbLibDir)\libbid.lib')">libbid.lib;</HeptaIbAdditionalDependencies>
-  </PropertyGroup>
-  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Debug|Win32'">
+  <Import Project="..\build\Hepta.IB.props" Condition="Exists('..\build\Hepta.IB.props')" />
+<PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Debug|Win32'">
     <LinkIncremental>true</LinkIncremental>
   </PropertyGroup>
   <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Debug|x64'">
@@ -198,7 +184,8 @@ if exist "$(IBApiBin)\\*.dll" (copy "$(IBApiBin)\\*.dll" "$(SolutionDir)$(Platfo
   </ItemDefinitionGroup>
   <ItemGroup>
     <ClCompile Include="adapter_ib\ib_api_wrapper.cpp" />
-    <ClCompile Include="adapter_ib\ib_gateway_adapter.cpp" />
+    <ClCompile Include="adapter_ib\ib_gateway_adapter.cpp" />    <ClCompile Include="adapter_xt\xt_gateway_adapter.cpp" />
+    <ClCompile Include="adapter_ctp\ctp_gateway_adapter.cpp" />
     <ClCompile Include="ib_fx_multi_strategy.cpp" />
     <ClCompile Include="HeptaDemoStrategyTrader.cpp" />
     <ClCompile Include="oms_journal.cpp" />
@@ -242,3 +229,9 @@ if exist "$(IBApiBin)\\*.dll" (copy "$(IBApiBin)\\*.dll" "$(SolutionDir)$(Platfo
   </ImportGroup>
 </Project>
 
+
+
+
+
+
+
diff --git a/HeptaTrade/HeptaTrader_Linux.vcxproj b/HeptaTrade/HeptaTrader_Linux.vcxproj
index 08db349..ab12101 100644
--- a/HeptaTrade/HeptaTrader_Linux.vcxproj
+++ b/HeptaTrade/HeptaTrader_Linux.vcxproj
@@ -70,6 +70,8 @@
   </PropertyGroup>
   <ItemGroup>
     <ClCompile Include="HeptaDemoStrategyTrader.cpp" />
+    <ClCompile Include="oms_journal.cpp" />
+    <ClCompile Include="oms_recover.cpp" />
   </ItemGroup>
   <ItemDefinitionGroup Condition="'$(Configuration)|$(Platform)'=='Debug|x64'">
     <ClCompile>
diff --git a/docs/BUILD-HYGIENE.md b/docs/BUILD-HYGIENE.md
index 690fc4a..6839f16 100644
--- a/docs/BUILD-HYGIENE.md
+++ b/docs/BUILD-HYGIENE.md
@@ -23,3 +23,17 @@ powershell -ExecutionPolicy Bypass -File .\scripts\release_check.ps1
 ```
 - 蹇呴』 `OVERALL=PASS`
 - 鑻ュけ璐ワ紝浼樺厛澶勭悊 `RISK_CONFIG / IB_HEALTHCHECK / IB_REGRESSION_ROUND`
+
+## IB 鏋勫缓鏀舵暃锛堥槻鍛戒护姹℃煋锛?+- IB 鐩稿叧灞炴€х粺涓€鏀舵暃鍒?`build/Hepta.IB.props`锛岄伩鍏嶆墜宸ュ湪鍛戒护琛屾嫾鎺ュ涓?`/p:` 瀵艰嚧婕傜Щ銆?+- `HeptaTrade/HeptaTrader.vcxproj` 宸插惎鐢?`TreatAsLocalProperty`锛坄HeptaBuildProfile/IBApiRoot/IBApiBin/HeptaEnableIbApi`锛夛紝鍑忓皯鍏ㄥ眬灞炴€т覆鎵般€?+- `HeptaBuildProfile=ib-release` 鏃惰嚜鍔ㄥ惎鐢?IB锛沗IBApiRoot` 鑷姩鍙戠幇椤哄簭锛?+  1. `/p:IBApiRoot=...`
+  2. 鐜鍙橀噺 `IBAPI_ROOT`
+  3. 浠撳簱鍐?`Interface/IBApi/source/CppClient`
+- `libbid.lib` 涓?*鍙€?*渚濊禆锛氳嫢瀛樺湪鍒欒嚜鍔ㄩ摼鎺ワ紱涓嶅瓨鍦ㄦ椂鎻愮ず骞堕€€鍥?source-only IB 鏋勫缓锛屼笉涓柇缂栬瘧銆?+
+### 鍞竴鎺ㄨ崘鏋勫缓鍛戒护锛圵indows x64 Release + IB锛?+```powershell
+powershell -ExecutionPolicy Bypass -File .\scripts\build_release_ib.ps1
+```
diff --git a/docs/CI-GATE.md b/docs/CI-GATE.md
index 147ef5b..c57974d 100644
--- a/docs/CI-GATE.md
+++ b/docs/CI-GATE.md
@@ -1,6 +1,6 @@
-# CI Gate锛圥R Gate / Release Gate锛?+锘? CI Gate锛圥R Gate / Release Gate锛? 
-鐢ㄤ簬 PR 楠岃瘉涓庡彂甯冨墠闂ㄧ銆?+鐢ㄤ簬 PR 楠岃瘉涓庡彂甯冨墠闂ㄧ銆傚綋鍓嶅彂甯冮棬绂佹槑纭檺瀹?**IB + CTP**锛?*XT 涓嶅弬涓庢斁琛屽垽瀹?*銆? 
 - **PR Gate锛堝彲闄嶇骇锛?*锛歚scripts/ci_gate_pr.ps1`
 - **Release Gate锛堝己鍒剁湡瀹為棬绂侊級**锛歚scripts/ci_gate_release.ps1`
@@ -40,14 +40,21 @@ powershell -ExecutionPolicy Bypass -File .\gate-local.ps1 -NoLaunch -SkipHealthc
 powershell -ExecutionPolicy Bypass -File .\scripts\ci_gate_release.ps1 -ProjectRoot "$PWD"
 ```
 
-### Release Gate 纭€ц姹傦紙鏂板锛?+### Release Gate 纭€ц姹傦紙IB+CTP only锛? 
-Release gate 閫氳繃鏉′欢蹇呴』鍚屾椂婊¤冻锛?+Release gate 閫氳繃鏉′欢蹇呴』鍚屾椂婊¤冻锛堝叏閮?`pass=true` 涓旈潪 `skipped`锛夛細
 
-1. **IB/CTP 鑷冲皯涓€涓湡瀹炲洖褰掗€氳繃**锛坄IB_REGRESSION_ROUND` 鎴?`CTP_REGRESSION_ROUND` 涓嚦灏戜竴涓?`pass=true` 涓旈潪 `skipped`锛?-2. **鍚姩瀵硅处鎶ュ憡鏃?CRITICAL**锛坄runtime-logs/reconcile_startup_report.json` 鎴?`HEPTA_RECONCILE_REPORT_PATH` 鎸囧畾鎶ュ憡涓?`startup_action.has_critical=false`锛?+1. `BUILD`锛坄HeptaTrader.sln` Debug|x64锛?+2. `IB_HEALTHCHECK`
+3. `IB_REGRESSION_ROUND`
+4. `CTP_REGRESSION_ROUND`
+5. `RECONCILE_CRITICAL_BLOCK`锛堝惎鍔ㄥ璐︽姤鍛婁笉寰楁湁 CRITICAL锛? 
-鍥犳锛?*绂佹鈥滀粎 no-gateway 閫氳繃鍗冲彂甯冣€?*銆?+鍥犳锛屽彂甯冮棬绂佷笉鍏佽锛?+
+- 浠?no-gateway 閫氳繃灏辨斁琛?+- 璺宠繃 IB/CTP 鍥炲綊涓殑浠讳竴椤?+- 渚濊禆 XT 妫€鏌ョ粨鏋滀綔涓烘斁琛屼緷鎹? 
 ### Release Gate 鏄庣‘绂佹
 
@@ -58,39 +65,61 @@ Release gate 閫氳繃鏉′欢蹇呴』鍚屾椂婊¤冻锛? 
 ---
 
-## 3) 澶辫触閫€鍑虹爜
+## 3) 鏈哄櫒鍙杈撳嚭锛圝SON 鎶ュ憡锛?+
+姣忔鎵ц `ci_gate*.ps1` 閮戒細鐢熸垚锛?+
+- `runtime-logs/ci-gate-<timestamp>/ci_gate_summary.json`
+- `runtime-logs/ci-gate-<timestamp>/ci_gate_summary.txt`
+
+绀轰緥璺緞锛?+
+- `runtime-logs/ci-gate-20260228-181530/ci_gate_summary.json`
+- `runtime-logs/ci-gate-20260228-181530/build.stdout.log`
+- `runtime-logs/ci-gate-20260228-181530/healthcheck.stdout.log`
+- `runtime-logs/ci-gate-20260228-181530/ib_regression.stdout.log`
+- `runtime-logs/ci-gate-20260228-181530/ctp_regression.stdout.log`
+- `runtime-logs/ci-gate-20260228-181530/reconcile.stdout.log`
+
+`ci_gate_summary.json` 鍏抽敭瀛楁锛?+
+- `overall`: `PASS|FAIL`
+- `exitCode`: 闂ㄧ閫€鍑虹爜
+- `scope`: `IB+CTP`
+- `excludes`: `["XT"]`
+- `checks[]`: 姣忎釜妫€鏌ラ」鐨?`name/pass/exitCode/detail/artifacts`
+
+---
+
+## 4) 澶辫触閫€鍑虹爜
 
 ### `ci_gate_pr.ps1` / `ci_gate.ps1`
 
 - `0`锛氬叏閮ㄩ€氳繃
+- `10`锛欱uild 澶辫触
 - `11`锛歐hitelist 澶辫触
 - `12`锛歊egression 澶辫触
-- `13`锛歊elease check 澶辫触
+- `13`锛欼B Healthcheck 澶辫触
+- `16`锛氬璐︽姤鍛婄己澶辨垨瀛樺湪 CRITICAL
 - `90`锛氳剼鏈己澶? - `99`锛氭湭棰勬湡寮傚父
 
 ### `ci_gate_release.ps1`
 
 - `0`锛氬叏閮ㄩ€氳繃
+- `10`锛欱uild 澶辫触
 - `11`锛歐hitelist 澶辫触
 - `12`锛歊egression 澶辫触
-- `13`锛歊elease check 澶辫触
+- `13`锛欼B Healthcheck 澶辫触
 - `14`锛氱姝㈠弬鏁帮紙`-SkipHealthcheck` / `-SkipRegression`锛?-- `15`锛氭湭婊¤冻鈥滆嚦灏戜竴涓湡瀹?IB/CTP 鍥炲綊閫氳繃鈥?+- `15`锛氬繀闇€妫€鏌ョ己澶辨垨琚烦杩? - `16`锛氬璐︽姤鍛婄己澶辨垨瀛樺湪 CRITICAL
 - `90`锛氳剼鏈己澶? - `99`锛氭湭棰勬湡寮傚父
 
 ---
 
-## 4) 闃茶鐢ㄨ鏄?-
-- GitHub Actions 榛樿 `ci-gate.yml` 杩愯鐨勬槸 **PR Gate**锛岀敤浜庝唬鐮侀獙璇侊紝涓嶄唬琛ㄥ彂甯冩斁琛屻€?-- **鍙戝竷鏀捐蹇呴』鏄惧紡杩愯 `ci_gate_release.ps1`**锛堝叿澶囩湡瀹炲洖褰?+ 瀵硅处妫€鏌ワ級銆?-
----
-
-## 5) 楠岃瘉锛堝惈鏁呮剰澶辫触鏍蜂緥锛?+## 5) 鍙墽琛屽懡浠わ紙鍚獙璇佹牱渚嬶級
 
 ### 姝ｅ父鍙戝竷闂ㄧ
 
@@ -98,27 +127,18 @@ Release gate 閫氳繃鏉′欢蹇呴』鍚屾椂婊¤冻锛? powershell -ExecutionPolicy Bypass -File .\scripts\ci_gate_release.ps1 -ProjectRoot "$PWD"
 ```
 
-### 鏁呮剰澶辫触鏍蜂緥锛堥獙璇佺姝?skip锛?+### 浠呮牎楠?summary 绛栫暐锛堜笉閲嶈窇闂ㄧ锛? 
 ```powershell
-powershell -ExecutionPolicy Bypass -File .\scripts\ci_gate_release.ps1 -ProjectRoot "$PWD" -SkipRegression
-# 棰勬湡锛歟xit code = 14
+powershell -ExecutionPolicy Bypass -File .\scripts\ci_gate_release.ps1 `
+  -ProjectRoot "$PWD" `
+  -PolicyCheckOnly `
+  -GateSummaryPath .\runtime-logs\ci-gate-20260228-181530\ci_gate_summary.json
 ```
 
-### 鏁呮剰澶辫触鏍蜂緥锛堥獙璇佲€滄棤鐪熷疄鍥炲綊鈥濈瓥鐣ワ紝policy-check-only锛?+### 鏁呮剰澶辫触鏍蜂緥锛堥獙璇佺姝?skip锛? 
 ```powershell
-@'
-{
-  "checks": [
-    { "name": "IB_REGRESSION_ROUND", "pass": true, "detail": "skipped by -SkipRegression" }
-  ]
-}
-'@ | Out-File -Encoding utf8 .\runtime-logs\fake_ci_gate_summary.json
-
-powershell -ExecutionPolicy Bypass -File .\scripts\ci_gate_release.ps1 `
-  -ProjectRoot "$PWD" `
-  -PolicyCheckOnly `
-  -GateSummaryPath .\runtime-logs\fake_ci_gate_summary.json
-# 棰勬湡锛歟xit code = 15
+powershell -ExecutionPolicy Bypass -File .\scripts\ci_gate_release.ps1 -ProjectRoot "$PWD" -SkipRegression
+# 棰勬湡锛歟xit code = 14
 ```
diff --git a/scripts/ci_gate.ps1 b/scripts/ci_gate.ps1
index 5068ebc..3bcf2ca 100644
--- a/scripts/ci_gate.ps1
+++ b/scripts/ci_gate.ps1
@@ -1,4 +1,4 @@
-param(
+锘縫aram(
   [string]$ProjectRoot = 'D:\quant\HeptaTrader-master',
   [string]$ConfigPath,
   [ValidateSet('sim','paper','live')]
@@ -9,228 +9,113 @@ param(
   [int]$HealthTimeoutSec = 45,
   [int]$RegressionTimeoutSec = 90,
   [switch]$NoLaunch,
-  [switch]$SkipHealthcheck,
-  [switch]$SkipRegression,
   [switch]$Strict
 )
 
 $ErrorActionPreference = 'Stop'
 
-$ExitCodes = [ordered]@{
-  PASS = 0
-  WHITELIST_FAIL = 11
-  REGRESSION_FAIL = 12
-  RELEASE_CHECK_FAIL = 13
-  SCRIPT_MISSING = 90
-  UNEXPECTED_ERROR = 99
-}
-
-function Invoke-Step {
+function Run-PsScript {
   param(
     [string]$Name,
     [string]$File,
     [string[]]$Args,
-    [string]$StdoutPath,
-    [string]$StderrPath,
+    [string]$Stdout,
+    [string]$Stderr,
     [int]$TimeoutSec = 0
   )
 
   if(!(Test-Path $File)){
-    return [ordered]@{ name = $Name; pass = $false; exitCode = $ExitCodes.SCRIPT_MISSING; detail = "Missing script: $File"; stdout = $StdoutPath; stderr = $StderrPath }
+    return [pscustomobject]@{ name=$Name; pass=$false; exitCode=90; detail="missing:$File"; artifacts=@($Stdout,$Stderr) }
   }
 
   $argList = @('-NoProfile','-ExecutionPolicy','Bypass','-File',$File) + $Args
-  $p = Start-Process -FilePath 'powershell' -ArgumentList $argList -PassThru -RedirectStandardOutput $StdoutPath -RedirectStandardError $StderrPath
-
+  $p = Start-Process -FilePath 'powershell' -ArgumentList $argList -PassThru -RedirectStandardOutput $Stdout -RedirectStandardError $Stderr
   if($TimeoutSec -gt 0){
-    $ok = $p.WaitForExit($TimeoutSec * 1000)
-    if(-not $ok){
-      try { Stop-Process -Id $p.Id -Force -ErrorAction Stop } catch {}
-      return [ordered]@{ name = $Name; pass = $false; exitCode = 124; detail = "timeout after ${TimeoutSec}s"; stdout = $StdoutPath; stderr = $StderrPath }
+    if(-not $p.WaitForExit($TimeoutSec*1000)){
+      Stop-Process -Id $p.Id -Force -ErrorAction SilentlyContinue
+      return [pscustomobject]@{ name=$Name; pass=$false; exitCode=99; detail="timeout:${TimeoutSec}s"; artifacts=@($Stdout,$Stderr) }
     }
   } else {
     $p.WaitForExit()
   }
 
-  $code = $p.ExitCode
-  if($null -eq $code){
-    $raw = ''
-    if(Test-Path $StdoutPath){ $raw = Get-Content -LiteralPath $StdoutPath -Raw -ErrorAction SilentlyContinue }
-    if($raw -match 'OVERALL=PASS'){ $code = 0 }
-    elseif($raw -match 'OVERALL=FAIL'){ $code = 1 }
-  }
-
-  return [ordered]@{ name = $Name; pass = ($code -eq 0); exitCode = $code; detail = "exitCode=$code"; stdout = $StdoutPath; stderr = $StderrPath }
+  $exit = [int]$p.ExitCode
+  return [pscustomobject]@{ name=$Name; pass=([bool]($exit -eq 0)); exitCode=$exit; detail=''; artifacts=@($Stdout,$Stderr) }
 }
 
-function Resolve-Python([string]$Preferred){
-  $cmd = Get-Command $Preferred -ErrorAction SilentlyContinue
-  if($cmd){ return $Preferred }
-
-  $py = Get-Command py -ErrorAction SilentlyContinue
-  if($py){ return 'py' }
-
-  return $null
+$ts = Get-Date -Format 'yyyyMMdd-HHmmss'
+$gateDir = Join-Path $ProjectRoot "runtime-logs\ci-gate-$ts"
+New-Item -ItemType Directory -Force -Path $gateDir | Out-Null
+
+$summaryJson = Join-Path $gateDir 'ci_gate_summary.json'
+$summaryTxt = Join-Path $gateDir 'ci_gate_summary.txt'
+
+$checks = @()
+
+# BUILD
+$buildOut = Join-Path $gateDir 'build.stdout.log'
+$buildErr = Join-Path $gateDir 'build.stderr.log'
+$msbuild = 'D:\VSstudio\MSBuild\Current\Bin\amd64\MSBuild.exe'
+if(!(Test-Path $msbuild)){
+  $checks += [pscustomobject]@{ name='BUILD'; pass=$false; exitCode=90; detail='msbuild_missing'; artifacts=@($buildOut,$buildErr) }
+} else {
+  $b = Start-Process -FilePath $msbuild -ArgumentList @('HeptaTrader.sln','/t:Build','/p:Configuration=Release','/p:Platform=x64','/m') -WorkingDirectory $ProjectRoot -PassThru -RedirectStandardOutput $buildOut -RedirectStandardError $buildErr
+  $b.WaitForExit()
+  $bExit = [int]$b.ExitCode
+   $checks += [pscustomobject]@{ name='BUILD'; pass=([bool]($bExit -eq 0)); exitCode=$bExit; detail=''; artifacts=@($buildOut,$buildErr) }
 }
 
-try {
-  $PythonExe = Resolve-Python $PythonExe
-  if([string]::IsNullOrWhiteSpace($PythonExe)){
-    throw 'Python not found. Install python or py launcher.'
-  }
-
-  $runtimeRoot = Join-Path $ProjectRoot 'runtime-logs'
-  New-Item -ItemType Directory -Path $runtimeRoot -Force | Out-Null
-  $ts = Get-Date -Format 'yyyyMMdd-HHmmss'
-  $gateDir = Join-Path $runtimeRoot ("ci-gate-$ts")
-  New-Item -ItemType Directory -Path $gateDir -Force | Out-Null
-
-  $resolveScript = Join-Path $ProjectRoot 'scripts\resolve_hepta_config.py'
-  if(!(Test-Path $resolveScript)){
-    throw "Missing resolver script: $resolveScript"
-  }
-
-  $resolveArgs = @()
-  if($PythonExe -eq 'py'){ $resolveArgs += '-3' }
-  $resolveArgs += @($resolveScript, '--project-root', $ProjectRoot)
-  if(-not [string]::IsNullOrWhiteSpace($ConfigPath)){ $resolveArgs += @('--config', $ConfigPath) }
-  if(-not [string]::IsNullOrWhiteSpace($Profile)){ $resolveArgs += @('--profile', $Profile) }
-
-  $resolveRaw = & $PythonExe @resolveArgs
-  if($LASTEXITCODE -ne 0){ throw 'resolve_hepta_config.py failed.' }
-  $resolved = $resolveRaw | ConvertFrom-Json
-  $ConfigPath = $resolved.config_path
-  $env:HEPTA_CONFIG_PATH = $resolved.config_path
-  $env:HEPTA_PROFILE = $resolved.profile
-  $env:HEPTA_CONFIG_SHA256 = $resolved.sha256
-  Write-Host ("CONFIG_FINGERPRINT config_path={0} profile={1} sha256={2}" -f $resolved.config_path, $resolved.profile, $resolved.sha256)
-
-  $checks = @()
-
-  # 1) whitelist check (standalone)
-  $whitelistScript = Join-Path $ProjectRoot 'scripts\check_ib_order_whitelist.py'
-  $wlOut = Join-Path $gateDir 'whitelist.stdout.log'
-  $wlErr = Join-Path $gateDir 'whitelist.stderr.log'
-
-  if(!(Test-Path $whitelistScript)){
-    $checks += [ordered]@{ name='WHITELIST'; pass=$false; exitCode=$ExitCodes.SCRIPT_MISSING; detail="Missing script: $whitelistScript"; stdout=$wlOut; stderr=$wlErr }
-    $overall = 'FAIL'
-    $finalExit = $ExitCodes.SCRIPT_MISSING
-  } else {
-    $pyArgs = @()
-    if($PythonExe -eq 'py'){ $pyArgs += '-3' }
-    $pyArgs += $whitelistScript
-    $p = Start-Process -FilePath $PythonExe -ArgumentList $pyArgs -PassThru -NoNewWindow -RedirectStandardOutput $wlOut -RedirectStandardError $wlErr
-    $p.WaitForExit()
-    $wlCode = $p.ExitCode
-    if($null -eq $wlCode){
-      $wlRaw = ''
-      if(Test-Path $wlOut){ $wlRaw = Get-Content -LiteralPath $wlOut -Raw -ErrorAction SilentlyContinue }
-      if($wlRaw -match '\[PASS\]'){ $wlCode = 0 } else { $wlCode = 1 }
-    }
-    $wlPass = ($wlCode -eq 0)
-    $checks += [ordered]@{ name='WHITELIST'; pass=$wlPass; exitCode=$wlCode; detail="exitCode=$wlCode"; stdout=$wlOut; stderr=$wlErr }
-
-    if(-not $wlPass){
-      $overall = 'FAIL'
-      $finalExit = $ExitCodes.WHITELIST_FAIL
-    }
-  }
-
-  # 2) ib regression round
-  if($null -eq $finalExit){
-    if($SkipRegression){
-      $checks += [ordered]@{ name='IB_REGRESSION_ROUND'; pass=$true; exitCode=0; detail='skipped by -SkipRegression'; stdout=''; stderr='' }
-    } else {
-      $regScript = Join-Path $ProjectRoot 'scripts\run_ib_regression_round.ps1'
-      $res = Invoke-Step -Name 'IB_REGRESSION_ROUND' -File $regScript -Args @(
-        '-ProjectRoot', $ProjectRoot,
-        '-IbHost', $IbHost,
-        '-Port', "$Port",
-        '-TimeoutSec', "$RegressionTimeoutSec",
-        '-PythonExe', $PythonExe
-      ) -StdoutPath (Join-Path $gateDir 'regression.stdout.log') -StderrPath (Join-Path $gateDir 'regression.stderr.log') -TimeoutSec ($RegressionTimeoutSec + 60)
-
-      $checks += $res
-      if(-not $res.pass){
-        $overall = 'FAIL'
-        $finalExit = $ExitCodes.REGRESSION_FAIL
-      }
-    }
-  }
-
-  # 3) release_check (skip regression to avoid duplicate run)
-  if($null -eq $finalExit){
-    if($SkipHealthcheck -and $SkipRegression){
-      $checks += [ordered]@{ name='RELEASE_CHECK'; pass=$true; exitCode=0; detail='skipped by -SkipHealthcheck + -SkipRegression (no-gateway mode)'; stdout=''; stderr='' }
-    } else {
-      $releaseScript = Join-Path $ProjectRoot 'scripts\release_check.ps1'
-      $releaseArgs = @(
-        '-ProjectRoot', $ProjectRoot,
-        '-ConfigPath', $ConfigPath,
-        '-Profile', $resolved.profile,
-        '-PythonExe', $PythonExe,
-        '-IbHost', $IbHost,
-        '-Port', "$Port",
-        '-HealthTimeoutSec', "$HealthTimeoutSec",
-        '-SkipRegression'
-      )
-      if($NoLaunch){ $releaseArgs += '-NoLaunch' }
-      if($SkipHealthcheck){ $releaseArgs += '-SkipHealthcheck' }
-      if($Strict){ $releaseArgs += '-Strict' }
-
-      $res = Invoke-Step -Name 'RELEASE_CHECK' -File $releaseScript -Args $releaseArgs -StdoutPath (Join-Path $gateDir 'release.stdout.log') -StderrPath (Join-Path $gateDir 'release.stderr.log') -TimeoutSec ($HealthTimeoutSec + 120)
-      $checks += $res
-
-      if(-not $res.pass){
-        $overall = 'FAIL'
-        $finalExit = $ExitCodes.RELEASE_CHECK_FAIL
-      }
-    }
-  }
-
-  if($null -eq $finalExit){
-    $overall = 'PASS'
-    $finalExit = $ExitCodes.PASS
-  }
-
-  $summary = [ordered]@{
-    timestamp = (Get-Date).ToString('o')
-    overall = $overall
-    exitCode = $finalExit
-    exitCodeSpec = $ExitCodes
-    configPath = $resolved.config_path
-    profile = $resolved.profile
-    configSha256 = $resolved.sha256
-    gateDir = $gateDir
-    checks = $checks
-  }
-
-  $jsonPath = Join-Path $gateDir 'ci_gate_summary.json'
-  $txtPath = Join-Path $gateDir 'ci_gate_summary.txt'
-  $summary | ConvertTo-Json -Depth 8 | Out-File -FilePath $jsonPath -Encoding utf8
+# IB_HEALTHCHECK (release exe)
+$hcOut = Join-Path $gateDir 'healthcheck.stdout.log'
+$hcErr = Join-Path $gateDir 'healthcheck.stderr.log'
+$hcArgs = @('-ProjectRoot',$ProjectRoot,'-ExePath',(Join-Path $ProjectRoot 'x64\Release\HeptaTrader.exe'),'-WorkDir',(Join-Path $ProjectRoot 'x64\Release'),'-TimeoutSec',"$HealthTimeoutSec")
+if($NoLaunch){ $hcArgs += '-NoLaunch' }
+$checks += Run-PsScript -Name 'IB_HEALTHCHECK' -File (Join-Path $ProjectRoot 'scripts\ib_gateway_healthcheck.ps1') -Args $hcArgs -Stdout $hcOut -Stderr $hcErr -TimeoutSec ($HealthTimeoutSec+20)
+
+# IB_REGRESSION_ROUND
+$ibrOut = Join-Path $gateDir 'ib_regression.stdout.log'
+$ibrErr = Join-Path $gateDir 'ib_regression.stderr.log'
+$ibrArgs = @('-ProjectRoot',$ProjectRoot,'-PythonExe',$PythonExe,'-IbHost',$IbHost,'-Port',"$Port",'-TimeoutSec',"$RegressionTimeoutSec")
+$checks += Run-PsScript -Name 'IB_REGRESSION_ROUND' -File (Join-Path $ProjectRoot 'scripts\run_ib_regression_round.ps1') -Args $ibrArgs -Stdout $ibrOut -Stderr $ibrErr -TimeoutSec ($RegressionTimeoutSec+30)
+
+# CTP_REGRESSION_ROUND
+$ctpOut = Join-Path $gateDir 'ctp_regression.stdout.log'
+$ctpErr = Join-Path $gateDir 'ctp_regression.stderr.log'
+$checks += Run-PsScript -Name 'CTP_REGRESSION_ROUND' -File (Join-Path $ProjectRoot 'scripts\run_ctp_regression_round.ps1') -Args @() -Stdout $ctpOut -Stderr $ctpErr -TimeoutSec 120
+
+# RECONCILE_CRITICAL_BLOCK
+$recOut = Join-Path $gateDir 'reconcile.stdout.log'
+$recErr = Join-Path $gateDir 'reconcile.stderr.log'
+$checks += Run-PsScript -Name 'RECONCILE_CRITICAL_BLOCK' -File (Join-Path $ProjectRoot 'scripts\check_reconcile_critical_block.ps1') -Args @() -Stdout $recOut -Stderr $recErr -TimeoutSec 30
+
+$overall = $true
+foreach($c in $checks){ if(-not [bool]$c.pass){ $overall = $false } }
+
+$summary = [pscustomobject]@{
+  timestamp = $ts
+  scope = 'IB+CTP'
+  excludes = @('XT')
+  checks = $checks
+  overall = if($overall){'PASS'} else {'FAIL'}
+}
+$summary | ConvertTo-Json -Depth 6 | Set-Content $summaryJson -Encoding UTF8
+
+$lines = @()
+$lines += "OVERALL=$(if($overall){'PASS'}else{'FAIL'})"
+$lines += "EXIT_CODE=$(if($overall){0}else{10})"
+$lines += 'SCOPE=IB+CTP (XT excluded)'
+$lines += "SUMMARY_JSON=$summaryJson"
+$lines += "GATE_DIR=$gateDir"
+foreach($c in $checks){
+  $lines += "[$($c.name)] pass=$([bool]$c.pass) exit=$($c.exitCode) detail=$($c.detail)"
+}
+$lines | Set-Content $summaryTxt -Encoding UTF8
 
-  $lines = @(
-    "OVERALL=$overall",
-    "EXIT_CODE=$finalExit",
-    "CONFIG_FINGERPRINT config_path=$($summary.configPath) profile=$($summary.profile) sha256=$($summary.configSha256)",
-    "SUMMARY_JSON=$jsonPath",
-    "GATE_DIR=$gateDir"
-  )
-  foreach($c in $checks){
-    $tag = if($c.pass){ 'PASS' } else { 'FAIL' }
-    $lines += ("{0}={1} (exit={2})" -f $c.name, $tag, $c.exitCode)
-  }
-  $lines -join "`r`n" | Out-File -FilePath $txtPath -Encoding utf8
+Write-Output "OVERALL=$(if($overall){'PASS'}else{'FAIL'})"
+Write-Output "EXIT_CODE=$(if($overall){0}else{10})"
+Write-Output 'SCOPE=IB+CTP (XT excluded)'
+Write-Output "SUMMARY_JSON=$summaryJson"
+Write-Output "GATE_DIR=$gateDir"
 
-  Write-Host "OVERALL=$overall" -ForegroundColor $(if($overall -eq 'PASS'){ 'Green' } else { 'Red' })
-  Write-Host "EXIT_CODE=$finalExit"
-  Write-Host "SUMMARY_JSON=$jsonPath"
-  Write-Host "SUMMARY_TXT=$txtPath"
+if($overall){ exit 0 } else { exit 10 }
 
-  exit $finalExit
-}
-catch {
-  Write-Error $_
-  exit $ExitCodes.UNEXPECTED_ERROR
-}
diff --git a/scripts/ci_gate_release.ps1 b/scripts/ci_gate_release.ps1
index 262e76d..8966d29 100644
--- a/scripts/ci_gate_release.ps1
+++ b/scripts/ci_gate_release.ps1
@@ -1,4 +1,4 @@
-param(
+锘縫aram(
   [string]$ProjectRoot = 'D:\quant\HeptaTrader-master',
   [string]$ConfigPath,
   [ValidateSet('sim','paper','live')]
@@ -29,9 +29,10 @@ function Fail-AndExit {
 
 $ExitCodes = [ordered]@{
   PASS = 0
+  BUILD_FAIL = 10
   WHITELIST_FAIL = 11
   REGRESSION_FAIL = 12
-  RELEASE_CHECK_FAIL = 13
+  HEALTHCHECK_FAIL = 13
   FORBIDDEN_SKIP_OPTION = 14
   REAL_REGRESSION_REQUIRED = 15
   RECONCILE_CRITICAL_OR_MISSING = 16
@@ -67,9 +68,12 @@ function Resolve-LatestGateSummary {
   return $null
 }
 
+function Get-Check($summary, [string]$name){
+  return @($summary.checks | Where-Object { $_.name -eq $name } | Select-Object -First 1)
+}
+
 function Assert-ReleasePolicy {
   param(
-    [string]$Root,
     [string]$SummaryPath
   )
 
@@ -79,58 +83,32 @@ function Assert-ReleasePolicy {
 
   $summary = Get-Content -LiteralPath $SummaryPath -Raw | ConvertFrom-Json
 
-  $regressionChecks = @($summary.checks | Where-Object {
-      $_.name -in @('IB_REGRESSION_ROUND', 'CTP_REGRESSION_ROUND')
-    })
-  $realPassed = @($regressionChecks | Where-Object {
-      [bool]$_.pass -and (("$($_.detail)") -notmatch 'skipped')
-    })
-
-  if($realPassed.Count -lt 1){
-    $detail = if($regressionChecks.Count -eq 0){
-      'No IB/CTP regression checks found in ci_gate summary.'
-    } else {
-      ($regressionChecks | ForEach-Object { "{0}: pass={1}, detail={2}" -f $_.name, $_.pass, $_.detail }) -join '; '
+  $requiredChecks = @('BUILD','IB_HEALTHCHECK','IB_REGRESSION_ROUND','CTP_REGRESSION_ROUND','RECONCILE_CRITICAL_BLOCK')
+  foreach($checkName in $requiredChecks){
+    $c = Get-Check -summary $summary -name $checkName
+    if($null -eq $c){
+      Fail-AndExit -Code $ExitCodes.REAL_REGRESSION_REQUIRED -Message "Release policy check failed: missing check '$checkName' in $SummaryPath"
+    }
+    if(-not [bool]$c.pass){
+      $code = if($checkName -eq 'RECONCILE_CRITICAL_BLOCK'){ $ExitCodes.RECONCILE_CRITICAL_OR_MISSING } elseif($checkName -eq 'BUILD'){ $ExitCodes.BUILD_FAIL } elseif($checkName -eq 'IB_HEALTHCHECK'){ $ExitCodes.HEALTHCHECK_FAIL } else { $ExitCodes.REGRESSION_FAIL }
+      Fail-AndExit -Code $code -Message "Release policy check failed: $checkName pass=false detail=$($c.detail)"
+    }
+    if(("$($c.detail)") -match 'skipped'){
+      Fail-AndExit -Code $ExitCodes.REAL_REGRESSION_REQUIRED -Message "Release policy check failed: $checkName cannot be skipped in release gate."
     }
-    Fail-AndExit -Code $ExitCodes.REAL_REGRESSION_REQUIRED -Message "Release policy check failed (code=$($ExitCodes.REAL_REGRESSION_REQUIRED)): require at least one REAL passed regression (IB/CTP). $detail"
-  }
-
-  $reconcilePath = if(-not [string]::IsNullOrWhiteSpace($env:HEPTA_RECONCILE_REPORT_PATH)){
-    $env:HEPTA_RECONCILE_REPORT_PATH
-  } else {
-    Join-Path $Root 'runtime-logs\reconcile_startup_report.json'
-  }
-
-  if(!(Test-Path $reconcilePath)){
-    Fail-AndExit -Code $ExitCodes.RECONCILE_CRITICAL_OR_MISSING -Message "Release policy check failed (code=$($ExitCodes.RECONCILE_CRITICAL_OR_MISSING)): reconcile report missing: $reconcilePath"
-  }
-
-  $recon = Get-Content -LiteralPath $reconcilePath -Raw | ConvertFrom-Json
-  $hasCritical = $false
-  if($null -ne $recon.startup_action -and $null -ne $recon.startup_action.has_critical){
-    $hasCritical = [bool]$recon.startup_action.has_critical
-  } elseif($null -ne $recon.checks){
-    $critCount = @($recon.checks | Where-Object { "$($_.severity)" -eq 'CRITICAL' }).Count
-    $hasCritical = ($critCount -gt 0)
-  } elseif("$($recon.status)" -eq 'CRITICAL'){
-    $hasCritical = $true
-  }
-
-  if($hasCritical){
-    Fail-AndExit -Code $ExitCodes.RECONCILE_CRITICAL_OR_MISSING -Message "Release policy check failed (code=$($ExitCodes.RECONCILE_CRITICAL_OR_MISSING)): reconcile report has CRITICAL. report=$reconcilePath"
   }
 
-  Write-Host "RELEASE_POLICY=PASS"
-  Write-Host "RELEASE_POLICY_REAL_REGRESSION=PASS :: $($realPassed[0].name)"
-  Write-Host "RELEASE_POLICY_RECONCILE=PASS :: report=$reconcilePath"
+  Write-Host 'RELEASE_POLICY=PASS'
+  Write-Host 'RELEASE_POLICY_SCOPE=IB+CTP_ONLY (XT excluded)'
+  Write-Host "RELEASE_POLICY_SUMMARY_JSON=$SummaryPath"
 }
 
 if($SkipHealthcheck -or $SkipRegression){
-  Fail-AndExit -Code $ExitCodes.FORBIDDEN_SKIP_OPTION -Message 'ci_gate_release.ps1 forbids -SkipHealthcheck / -SkipRegression. Real gate must execute full healthcheck + regression.'
+  Fail-AndExit -Code $ExitCodes.FORBIDDEN_SKIP_OPTION -Message 'ci_gate_release.ps1 forbids -SkipHealthcheck / -SkipRegression. Real gate must execute build + healthcheck + IB/CTP regression + reconcile check.'
 }
 
 if($PolicyCheckOnly){
-  Assert-ReleasePolicy -Root $ProjectRoot -SummaryPath $GateSummaryPath
+  Assert-ReleasePolicy -SummaryPath $GateSummaryPath
   exit $ExitCodes.PASS
 }
 
@@ -166,5 +144,5 @@ $resolvedSummaryPath = if(-not [string]::IsNullOrWhiteSpace($GateSummaryPath)){
   if(-not [string]::IsNullOrWhiteSpace($fromOutput)){ $fromOutput } else { Resolve-LatestGateSummary -Root $ProjectRoot }
 }
 
-Assert-ReleasePolicy -Root $ProjectRoot -SummaryPath $resolvedSummaryPath
+Assert-ReleasePolicy -SummaryPath $resolvedSummaryPath
 exit $ExitCodes.PASS
diff --git a/scripts/run_ib_regression_round.ps1 b/scripts/run_ib_regression_round.ps1
index 69531b1..60b9f36 100644
--- a/scripts/run_ib_regression_round.ps1
+++ b/scripts/run_ib_regression_round.ps1
@@ -49,8 +49,15 @@ $checkStdout = Join-Path $roundDir 'whitelist_check.stdout.log'
 $checkStderr = Join-Path $roundDir 'whitelist_check.stderr.log'
 $checkProc = Start-Process -FilePath $PythonExe -ArgumentList $checkArgs -PassThru -NoNewWindow -RedirectStandardOutput $checkStdout -RedirectStandardError $checkStderr
 $checkProc.WaitForExit()
-if($checkProc.ExitCode -ne 0){
-  throw "IB order whitelist check failed. stdout=$checkStdout stderr=$checkStderr"
+$checkExit = $checkProc.ExitCode
+if($null -eq $checkExit){
+  # Some Python launcher/process combinations may leave ExitCode null even after WaitForExit.
+  # Fallback to stdout marker to avoid false negatives.
+  $checkOutText = if(Test-Path $checkStdout){ Get-Content -LiteralPath $checkStdout -Raw -ErrorAction SilentlyContinue } else { '' }
+  if($checkOutText -match '\[PASS\]'){ $checkExit = 0 } else { $checkExit = -1 }
+}
+if($checkExit -ne 0){
+  throw "IB order whitelist check failed. stdout=$checkStdout stderr=$checkStderr exitCode=$checkExit"
 }
 
 $args = @()

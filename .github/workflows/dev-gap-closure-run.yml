name: dev-gap-closure-run

on:
  push:
    branches:
      - codex/heptatrader-gap-closure-v2
    paths:
      - .github/workflows/dev-gap-closure-run.yml

permissions:
  contents: write

concurrency:
  group: dev-gap-closure-run
  cancel-in-progress: false

jobs:
  close-gaps:
    if: github.repository == 'TrillionniumFoundation/heptatrader' && github.event.head_commit.message == 'chore(dev): launch permit and observability gap closure'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout exact branch
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: codex/heptatrader-gap-closure-v2
          fetch-depth: 0
          persist-credentials: true

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install --yes --no-install-recommends cmake ninja-build g++ libssl-dev python3

      - name: Apply permit lifecycle tests and runtime telemetry wiring
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import re

          ROOT = Path.cwd()

          def read(path: str) -> str:
              return (ROOT / path).read_text(encoding="utf-8-sig")

          def write(path: str, text: str) -> None:
              target = ROOT / path
              target.parent.mkdir(parents=True, exist_ok=True)
              target.write_text(text, encoding="utf-8")

          def replace_once(path: str, old: str, new: str) -> None:
              text = read(path)
              if new in text:
                  return
              count = text.count(old)
              if count != 1:
                  raise RuntimeError(f"{path}: expected one replacement marker, found {count}: {old[:80]!r}")
              write(path, text.replace(old, new, 1))

          def add_include(path: str, anchor: str, include: str) -> None:
              text = read(path)
              if include in text:
                  return
              if anchor not in text:
                  raise RuntimeError(f"{path}: include anchor missing: {anchor}")
              write(path, text.replace(anchor, anchor + "\n" + include, 1))

          def insert_function_prefix(path: str, signature: str, code: str) -> None:
              text = read(path)
              if code.strip() in text:
                  return
              start = text.find(signature)
              if start < 0:
                  raise RuntimeError(f"{path}: function signature missing: {signature}")
              brace = text.find("{", start)
              if brace < 0:
                  raise RuntimeError(f"{path}: function opening brace missing: {signature}")
              write(path, text[:brace + 1] + "\n" + code + text[brace + 1:])

          telemetry_h = r'''#pragma once

#include <array>
#include <chrono>
#include <cstddef>
#include <cstdint>
#include <map>
#include <mutex>
#include <string>

struct RuntimeHistogramSnapshot
{
    std::uint64_t count = 0;
    std::uint64_t totalMicros = 0;
    std::uint64_t maxMicros = 0;
    std::array<std::uint64_t, 12> buckets{};
};

// Process-local, bounded telemetry for deterministic runtime transitions.
// Labels are allow-listed and sanitized before entering a series key. Account,
// credential, token, prompt, free-form detail and raw strategy text are never
// accepted by the typed recording helpers below.
class RuntimeTelemetry
{
public:
    static RuntimeTelemetry& Global();

    void IncrementKey(const std::string& metric,
                      const std::string& boundedLabels = std::string());
    void SetGaugeKey(const std::string& metric,
                     double value,
                     const std::string& boundedLabels = std::string());
    void ObserveLatencyKey(const std::string& metric,
                           std::uint64_t micros,
                           const std::string& boundedLabels = std::string());

    std::string SnapshotJson() const;
    std::size_t SeriesCount() const;
    void ResetForTests();

    static std::string BoundedLabel(const std::string& value);

private:
    RuntimeTelemetry() = default;
    static std::string Key(const std::string& metric,
                           const std::string& labels);

    static const std::size_t kMaximumSeries = 2048;
    mutable std::mutex m_mutex;
    std::map<std::string, std::uint64_t> m_counters;
    std::map<std::string, double> m_gauges;
    std::map<std::string, RuntimeHistogramSnapshot> m_histograms;
    std::uint64_t m_droppedSeries = 0;
};

class RuntimeLatencyScope
{
public:
    RuntimeLatencyScope(const std::string& metric,
                        const std::string& labelName = std::string(),
                        const std::string& labelValue = std::string());
    ~RuntimeLatencyScope();

    RuntimeLatencyScope(const RuntimeLatencyScope&) = delete;
    RuntimeLatencyScope& operator=(const RuntimeLatencyScope&) = delete;

private:
    std::string m_metric;
    std::string m_labels;
    std::chrono::steady_clock::time_point m_started;
};

void RuntimeRecordRiskDecision(bool allowed, const std::string& reasonCode);
void RuntimeRecordToolOutcome(const std::string& tool,
                              int status,
                              const std::string& reasonCode);
void RuntimeRecordJournalEvent(const std::string& eventType,
                               const std::string& status,
                               const std::string& reasonCode);
void RuntimeRecordJournalFailure(const std::string& reasonCode);
void RuntimeRecordReconcile(const std::string& operation,
                            const std::string& outcome,
                            const std::string& reasonCode);
void RuntimeRecordKillSwitch(const std::string& state);
'''

          telemetry_cpp = r'''#include "runtime_telemetry.h"

#include <algorithm>
#include <cmath>
#include <iomanip>
#include <limits>
#include <sstream>

namespace
{
const std::uint64_t kLatencyBuckets[12] = {
    10, 50, 100, 500, 1000, 5000,
    10000, 50000, 100000, 500000, 1000000, 5000000
};

std::uint64_t Fnv1a(const std::string& value)
{
    std::uint64_t hash = 1469598103934665603ULL;
    for (std::string::const_iterator it = value.begin(); it != value.end(); ++it)
    {
        hash ^= static_cast<unsigned char>(*it);
        hash *= 1099511628211ULL;
    }
    return hash;
}

std::string Hex64(std::uint64_t value)
{
    std::ostringstream out;
    out << 'h' << std::hex << std::setw(16) << std::setfill('0') << value;
    return out.str();
}

bool SafeMetricName(const std::string& value)
{
    if (value.empty() || value.size() > 96) return false;
    for (std::size_t i = 0; i < value.size(); ++i)
    {
        const char c = value[i];
        if (!((c >= 'a' && c <= 'z') || (c >= '0' && c <= '9') || c == '_'))
            return false;
    }
    return true;
}

std::string EscapeJson(const std::string& value)
{
    std::ostringstream out;
    for (std::string::const_iterator it = value.begin(); it != value.end(); ++it)
    {
        const unsigned char c = static_cast<unsigned char>(*it);
        if (c == '"') out << "\\\"";
        else if (c == '\\') out << "\\\\";
        else if (c < 0x20) out << '?';
        else out << *it;
    }
    return out.str();
}

std::string Labels1(const char* name, const std::string& value)
{
    return std::string(name) + '=' + RuntimeTelemetry::BoundedLabel(value);
}

std::string Labels2(const char* firstName, const std::string& firstValue,
                    const char* secondName, const std::string& secondValue)
{
    return Labels1(firstName, firstValue) + '|' +
        Labels1(secondName, secondValue);
}

std::string Labels3(const char* firstName, const std::string& firstValue,
                    const char* secondName, const std::string& secondValue,
                    const char* thirdName, const std::string& thirdValue)
{
    return Labels2(firstName, firstValue, secondName, secondValue) + '|' +
        Labels1(thirdName, thirdValue);
}

std::string StatusName(int status)
{
    switch (status)
    {
    case 0: return "ok";
    case 1: return "permission_denied";
    case 2: return "invalid_tool";
    case 3: return "rejected";
    case 4: return "duplicate";
    case 5: return "uncertain";
    case 6: return "error";
    default: return "unknown";
    }
}

bool Contains(const std::string& value, const char* token)
{
    return value.find(token) != std::string::npos;
}
}

RuntimeTelemetry& RuntimeTelemetry::Global()
{
    static RuntimeTelemetry telemetry;
    return telemetry;
}

std::string RuntimeTelemetry::BoundedLabel(const std::string& value)
{
    if (value.empty()) return "none";
    bool safe = value.size() <= 96;
    for (std::size_t i = 0; safe && i < value.size(); ++i)
    {
        const unsigned char c = static_cast<unsigned char>(value[i]);
        safe = std::isalnum(c) || c == '_' || c == '-' || c == '.' || c == ':';
    }
    return safe ? value : Hex64(Fnv1a(value));
}

std::string RuntimeTelemetry::Key(const std::string& metric,
                                  const std::string& labels)
{
    if (!SafeMetricName(metric)) return std::string();
    return labels.empty() ? metric : metric + '{' + labels + '}';
}

void RuntimeTelemetry::IncrementKey(const std::string& metric,
                                    const std::string& boundedLabels)
{
    const std::string key = Key(metric, boundedLabels);
    if (key.empty()) return;
    std::lock_guard<std::mutex> lock(m_mutex);
    std::map<std::string, std::uint64_t>::iterator found = m_counters.find(key);
    if (found == m_counters.end())
    {
        if (m_counters.size() + m_gauges.size() + m_histograms.size() >=
            kMaximumSeries)
        {
            ++m_droppedSeries;
            return;
        }
        found = m_counters.insert(std::make_pair(key, 0)).first;
    }
    if (found->second != std::numeric_limits<std::uint64_t>::max())
        ++found->second;
}

void RuntimeTelemetry::SetGaugeKey(const std::string& metric,
                                   double value,
                                   const std::string& boundedLabels)
{
    if (!std::isfinite(value)) return;
    const std::string key = Key(metric, boundedLabels);
    if (key.empty()) return;
    std::lock_guard<std::mutex> lock(m_mutex);
    if (m_gauges.find(key) == m_gauges.end() &&
        m_counters.size() + m_gauges.size() + m_histograms.size() >=
            kMaximumSeries)
    {
        ++m_droppedSeries;
        return;
    }
    m_gauges[key] = value;
}

void RuntimeTelemetry::ObserveLatencyKey(const std::string& metric,
                                         std::uint64_t micros,
                                         const std::string& boundedLabels)
{
    const std::string key = Key(metric, boundedLabels);
    if (key.empty()) return;
    std::lock_guard<std::mutex> lock(m_mutex);
    std::map<std::string, RuntimeHistogramSnapshot>::iterator found =
        m_histograms.find(key);
    if (found == m_histograms.end())
    {
        if (m_counters.size() + m_gauges.size() + m_histograms.size() >=
            kMaximumSeries)
        {
            ++m_droppedSeries;
            return;
        }
        found = m_histograms.insert(
            std::make_pair(key, RuntimeHistogramSnapshot())).first;
    }
    RuntimeHistogramSnapshot& sample = found->second;
    if (sample.count != std::numeric_limits<std::uint64_t>::max()) ++sample.count;
    sample.totalMicros =
        micros > std::numeric_limits<std::uint64_t>::max() - sample.totalMicros ?
        std::numeric_limits<std::uint64_t>::max() : sample.totalMicros + micros;
    sample.maxMicros = std::max(sample.maxMicros, micros);
    for (std::size_t i = 0; i < sample.buckets.size(); ++i)
        if (micros <= kLatencyBuckets[i] &&
            sample.buckets[i] != std::numeric_limits<std::uint64_t>::max())
            ++sample.buckets[i];
}

std::string RuntimeTelemetry::SnapshotJson() const
{
    std::lock_guard<std::mutex> lock(m_mutex);
    std::ostringstream out;
    out << "{\"schema\":\"hepta.runtime-metrics.v1\",\"counters\":{";
    bool first = true;
    for (std::map<std::string, std::uint64_t>::const_iterator it =
             m_counters.begin(); it != m_counters.end(); ++it)
    {
        if (!first) out << ',';
        first = false;
        out << '\"' << EscapeJson(it->first) << "\":" << it->second;
    }
    out << "},\"gauges\":{";
    first = true;
    for (std::map<std::string, double>::const_iterator it =
             m_gauges.begin(); it != m_gauges.end(); ++it)
    {
        if (!first) out << ',';
        first = false;
        out << '\"' << EscapeJson(it->first) << "\":"
            << std::setprecision(17) << it->second;
    }
    out << "},\"histograms\":{";
    first = true;
    for (std::map<std::string, RuntimeHistogramSnapshot>::const_iterator it =
             m_histograms.begin(); it != m_histograms.end(); ++it)
    {
        if (!first) out << ',';
        first = false;
        out << '\"' << EscapeJson(it->first) << "\":{\"count\":"
            << it->second.count << ",\"total_us\":" << it->second.totalMicros
            << ",\"max_us\":" << it->second.maxMicros << ",\"buckets\":[";
        for (std::size_t i = 0; i < it->second.buckets.size(); ++i)
        {
            if (i != 0) out << ',';
            out << it->second.buckets[i];
        }
        out << "]}";
    }
    out << "},\"dropped_series\":" << m_droppedSeries << '}';
    return out.str();
}

std::size_t RuntimeTelemetry::SeriesCount() const
{
    std::lock_guard<std::mutex> lock(m_mutex);
    return m_counters.size() + m_gauges.size() + m_histograms.size();
}

void RuntimeTelemetry::ResetForTests()
{
    std::lock_guard<std::mutex> lock(m_mutex);
    m_counters.clear();
    m_gauges.clear();
    m_histograms.clear();
    m_droppedSeries = 0;
}

RuntimeLatencyScope::RuntimeLatencyScope(
    const std::string& metric,
    const std::string& labelName,
    const std::string& labelValue)
    : m_metric(metric),
      m_labels(labelName.empty() ? std::string() :
          labelName + '=' + RuntimeTelemetry::BoundedLabel(labelValue)),
      m_started(std::chrono::steady_clock::now())
{
}

RuntimeLatencyScope::~RuntimeLatencyScope()
{
    const std::chrono::steady_clock::duration elapsed =
        std::chrono::steady_clock::now() - m_started;
    const std::uint64_t micros = static_cast<std::uint64_t>(
        std::chrono::duration_cast<std::chrono::microseconds>(elapsed).count());
    RuntimeTelemetry::Global().ObserveLatencyKey(m_metric, micros, m_labels);
}

void RuntimeRecordRiskDecision(bool allowed, const std::string& reasonCode)
{
    RuntimeTelemetry::Global().IncrementKey(
        "hepta_risk_decisions_total",
        Labels2("decision", allowed ? "allow" : "reject",
                "reason_code", reasonCode.empty() ? "RISK_REASON_MISSING" : reasonCode));
}

void RuntimeRecordToolOutcome(const std::string& tool,
                              int status,
                              const std::string& reasonCode)
{
    const std::string normalizedStatus = StatusName(status);
    RuntimeTelemetry::Global().IncrementKey(
        "hepta_tool_calls_total",
        Labels3("tool", tool, "status", normalizedStatus,
                "reason_code", reasonCode.empty() ? "none" : reasonCode));
    if (status == 1 || Contains(reasonCode, "SESSION") ||
        Contains(reasonCode, "PEER_") || Contains(reasonCode, "CAPABILITY"))
        RuntimeTelemetry::Global().IncrementKey(
            "hepta_session_rejections_total",
            Labels1("reason_code", reasonCode.empty() ?
                "SESSION_REJECTION_UNCLASSIFIED" : reasonCode));
}

void RuntimeRecordJournalEvent(const std::string& eventType,
                               const std::string& status,
                               const std::string& reasonCode)
{
    RuntimeTelemetry::Global().IncrementKey(
        "hepta_execution_events_total", Labels1("event_type", eventType));
    if (Contains(eventType, "send_attempt"))
    {
        std::string operation = Contains(eventType, "cancel") ? "cancel" :
            (Contains(eventType, "flatten") ? "flatten" : "place");
        RuntimeTelemetry::Global().IncrementKey(
            "hepta_venue_sends_total",
            Labels2("operation", operation, "status", "attempt"));
    }
    if (!status.empty())
        RuntimeTelemetry::Global().IncrementKey(
            "hepta_execution_commands_total",
            Labels2("status", status, "reason_code",
                    reasonCode.empty() ? "none" : reasonCode));
    if (Contains(eventType, "projection_failed") ||
        Contains(eventType, "state_break"))
        RuntimeTelemetry::Global().IncrementKey(
            "hepta_state_breaks_total", Labels1("kind", eventType));
    if (Contains(eventType, "reconcile") ||
        Contains(eventType, "projection_resolved") ||
        Contains(eventType, "command_resolved"))
        RuntimeRecordReconcile(eventType, status.empty() ? "observed" : status,
                               reasonCode);
}

void RuntimeRecordJournalFailure(const std::string& reasonCode)
{
    RuntimeTelemetry::Global().IncrementKey(
        "hepta_oms_journal_failures_total",
        Labels1("reason_code", reasonCode.empty() ?
            "OMS_FAILURE_UNCLASSIFIED" : reasonCode));
}

void RuntimeRecordReconcile(const std::string& operation,
                            const std::string& outcome,
                            const std::string& reasonCode)
{
    RuntimeTelemetry::Global().IncrementKey(
        "hepta_reconcile_runs_total",
        Labels3("operation", operation, "outcome", outcome,
                "reason_code", reasonCode.empty() ? "none" : reasonCode));
}

void RuntimeRecordKillSwitch(const std::string& state)
{
    RuntimeTelemetry::Global().IncrementKey(
        "hepta_kill_switch_transitions_total", Labels1("state", state));
}
'''

          write("HeptaTrade/observability/runtime_telemetry.h", telemetry_h)
          write("HeptaTrade/observability/runtime_telemetry.cpp", telemetry_cpp)

          cmake = read("HeptaTrade/CMakeLists.txt")
          if "add_library(hepta_observability_core STATIC" not in cmake:
              marker = "add_library(hepta_risk_core STATIC\n"
              if marker not in cmake:
                  raise RuntimeError("HeptaTrade/CMakeLists.txt: risk library marker missing")
              observability = (
                  "add_library(hepta_observability_core STATIC\n"
                  "    observability/runtime_telemetry.cpp)\n"
                  "hepta_runtime_target(hepta_observability_core)\n\n")
              cmake = cmake.replace(marker, observability + marker, 1)
          risk_target = "hepta_runtime_target(hepta_risk_core)"
          risk_link = risk_target + "\ntarget_link_libraries(hepta_risk_core PUBLIC hepta_observability_core)"
          if risk_link not in cmake:
              if risk_target not in cmake:
                  raise RuntimeError("HeptaTrade/CMakeLists.txt: risk target marker missing")
              cmake = cmake.replace(risk_target, risk_link, 1)
          agent_link_marker = "target_link_libraries(hepta_agent_os_core PUBLIC\n"
          if "target_link_libraries(hepta_agent_os_core PUBLIC\n    hepta_observability_core" not in cmake:
              if agent_link_marker not in cmake:
                  raise RuntimeError("HeptaTrade/CMakeLists.txt: agent link marker missing")
              cmake = cmake.replace(
                  agent_link_marker,
                  agent_link_marker + "    hepta_observability_core\n", 1)
          write("HeptaTrade/CMakeLists.txt", cmake)

          server_h = read("HeptaTrade/execution/unix_execution_service_server.h")
          if "friend class PreviewPermitTestAccess;" not in server_h:
              marker = "class UnixExecutionServiceServer\n{\n"
              if marker not in server_h:
                  raise RuntimeError("unix_execution_service_server.h: class marker missing")
              server_h = server_h.replace(
                  marker, marker + "    friend class PreviewPermitTestAccess;\n", 1)
              write("HeptaTrade/execution/unix_execution_service_server.h", server_h)

          add_include(
              "HeptaTrade/risk/deterministic_risk_policy.cpp",
              '#include "deterministic_risk_policy.h"',
              '#include "../observability/runtime_telemetry.h"')
          risk = read("HeptaTrade/risk/deterministic_risk_policy.cpp")
          allow_marker = '    decision.reasonCode = "RISK_OK";\n'
          if "RuntimeRecordRiskDecision(true" not in risk:
              if allow_marker not in risk:
                  raise RuntimeError("deterministic_risk_policy.cpp: allow marker missing")
              risk = risk.replace(
                  allow_marker,
                  allow_marker + '    RuntimeRecordRiskDecision(true, decision.reasonCode);\n', 1)
          reject_marker = "    decision.detail = detail;\n"
          if "RuntimeRecordRiskDecision(false" not in risk:
              if reject_marker not in risk:
                  raise RuntimeError("deterministic_risk_policy.cpp: reject marker missing")
              risk = risk.replace(
                  reject_marker,
                  reject_marker + '    RuntimeRecordRiskDecision(false, code);\n', 1)
          write("HeptaTrade/risk/deterministic_risk_policy.cpp", risk)
          insert_function_prefix(
              "HeptaTrade/risk/deterministic_risk_policy.cpp",
              "DeterministicRiskDecision DeterministicRiskPolicy::Evaluate(",
              '    RuntimeLatencyScope riskLatency("hepta_risk_decision_latency_microseconds");\n')

          add_include(
              "HeptaTrade/oms_journal.cpp",
              '#include "oms_journal.h"',
              '#include "observability/runtime_telemetry.h"')
          insert_function_prefix(
              "HeptaTrade/oms_journal.cpp",
              "bool OmsJournal::Append(",
              '    RuntimeLatencyScope appendLatency("hepta_oms_append_latency_microseconds");\n')
          insert_function_prefix(
              "HeptaTrade/oms_journal.cpp",
              "int OmsJournal::Replay(",
              '    RuntimeLatencyScope replayLatency("hepta_oms_replay_latency_microseconds");\n')
          journal = read("HeptaTrade/oms_journal.cpp")
          replacements = [
              (
                  "        m_writePoisoned = true;\n        ++m_writeFailTotal;\n        return false;\n    }\n    return true;\n}\n\nbool OmsJournal::WriteLineToPinnedFileLocked",
                  "        m_writePoisoned = true;\n        ++m_writeFailTotal;\n        RuntimeRecordJournalFailure(\"OMS_PATH_IDENTITY_INVALID\");\n        return false;\n    }\n    return true;\n}\n\nbool OmsJournal::WriteLineToPinnedFileLocked"
              ),
              (
                  "    if (m_fd < 0 || m_writePoisoned)\n    {\n        ++m_writeFailTotal;\n        return false;\n    }",
                  "    if (m_fd < 0 || m_writePoisoned)\n    {\n        ++m_writeFailTotal;\n        RuntimeRecordJournalFailure(\"OMS_JOURNAL_UNAVAILABLE\");\n        return false;\n    }"
              ),
              (
                  "            m_writePoisoned = true;\n            ++m_writeFailTotal;\n            return false;\n        }\n        offset +=",
                  "            m_writePoisoned = true;\n            ++m_writeFailTotal;\n            RuntimeRecordJournalFailure(\"OMS_WRITE_FAILED\");\n            return false;\n        }\n        offset +="
              ),
              (
                  "            ++m_writeFailTotal;\n            ++m_durableSyncFailures;\n            return false;",
                  "            ++m_writeFailTotal;\n            ++m_durableSyncFailures;\n            RuntimeRecordJournalFailure(\"OMS_FDATASYNC_FAILED\");\n            return false;"
              ),
          ]
          for old, new in replacements:
              if new in journal:
                  continue
              if old not in journal:
                  raise RuntimeError(f"oms_journal.cpp: telemetry marker missing: {old[:60]!r}")
              journal = journal.replace(old, new, 1)
          health_marker = "    out.writePoisoned = m_writePoisoned;\n    return out;"
          health_new = (
              "    out.writePoisoned = m_writePoisoned;\n"
              "    RuntimeTelemetry::Global().SetGaugeKey(\n"
              "        \"hepta_oms_journal_backlog\",\n"
              "        static_cast<double>(out.queueDepth + out.bufferedDepth));\n"
              "    RuntimeTelemetry::Global().SetGaugeKey(\n"
              "        \"hepta_oms_journal_write_failures\",\n"
              "        static_cast<double>(out.writeFailTotal));\n"
              "    RuntimeTelemetry::Global().SetGaugeKey(\n"
              "        \"hepta_oms_journal_poisoned\", out.writePoisoned ? 1.0 : 0.0);\n"
              "    return out;")
          if health_new not in journal:
              if health_marker not in journal:
                  raise RuntimeError("oms_journal.cpp: health marker missing")
              journal = journal.replace(health_marker, health_new, 1)
          write("HeptaTrade/oms_journal.cpp", journal)

          add_include(
              "HeptaTrade/execution/execution_coordinator.cpp",
              '#include "execution_coordinator.h"',
              '#include "../observability/runtime_telemetry.h"')
          coordinator = read("HeptaTrade/execution/execution_coordinator.cpp")
          append_old = (
              "    if (m_journal.Append(event)) return true;\n"
              "    BlockMutationsLocked(failureCode);\n"
              "    return false;")
          append_new = (
              "    if (m_journal.Append(event))\n"
              "    {\n"
              "        RuntimeRecordJournalEvent(\n"
              "            event.eventType, event.status, event.riskCode);\n"
              "        return true;\n"
              "    }\n"
              "    RuntimeRecordJournalFailure(failureCode);\n"
              "    BlockMutationsLocked(failureCode);\n"
              "    return false;")
          if append_new not in coordinator:
              if append_old not in coordinator:
                  raise RuntimeError("execution_coordinator.cpp: AppendOrBlock marker missing")
              coordinator = coordinator.replace(append_old, append_new, 1)
          write("HeptaTrade/execution/execution_coordinator.cpp", coordinator)

          add_include(
              "HeptaTrade/tool_host/unix_tool_server.cpp",
              '#include "typed_tool_protocol.h"',
              '#include "../observability/runtime_telemetry.h"')
          insert_function_prefix(
              "HeptaTrade/tool_host/unix_tool_server.cpp",
              "void UnixToolServer::ReplyAndClose(",
              "    RuntimeRecordToolOutcome(result.toolName,\n"
              "        static_cast<int>(result.status), result.reasonCode);\n")
          insert_function_prefix(
              "HeptaTrade/tool_host/unix_tool_server.cpp",
              "void UnixToolServer::Execute(",
              "    RuntimeLatencyScope toolLatency(\n"
              "        \"hepta_tool_execution_latency_microseconds\",\n"
              "        \"tool\", pending.request.call.name);\n")

          add_include(
              "HeptaTrade/execution/ib_paper_execution_profile.cpp",
              '#include "ib_paper_execution_profile.h"',
              '#include "../observability/runtime_telemetry.h"')
          profile = read("HeptaTrade/execution/ib_paper_execution_profile.cpp")
          kill_old = "    if (m_killSwitch->BlocksRiskIncrease(reason)) return false;"
          kill_new = (
              "    if (m_killSwitch->BlocksRiskIncrease(reason))\n"
              "    {\n"
              "        RuntimeRecordKillSwitch(\"blocked\");\n"
              "        return false;\n"
              "    }\n"
              "    RuntimeRecordKillSwitch(\"open\");")
          if kill_new not in profile:
              if kill_old not in profile:
                  raise RuntimeError("ib_paper_execution_profile.cpp: kill switch marker missing")
              profile = profile.replace(kill_old, kill_new, 1)
          write("HeptaTrade/execution/ib_paper_execution_profile.cpp", profile)

          permit_test = r'''#include "../HeptaTrade/execution/unix_execution_service_server.h"

#include <chrono>
#include <cmath>
#include <cstdlib>
#include <iostream>
#include <mutex>
#include <string>
#include <thread>

namespace
{
void Require(bool condition, const char* expression, int line)
{
    if (condition) return;
    std::cerr << "requirement failed at line " << line << ": "
              << expression << '\n';
    std::abort();
}
#define REQUIRE(expression) Require(static_cast<bool>(expression), #expression, __LINE__)

long long NowMs()
{
    return std::chrono::duration_cast<std::chrono::milliseconds>(
        std::chrono::system_clock::now().time_since_epoch()).count();
}

bool SamePlacePayload(const PlaceOrderCommand& left,
                      const PlaceOrderCommand& right)
{
    return left.context.agentId == right.context.agentId &&
        left.context.sessionId == right.context.sessionId &&
        left.context.toolCallId == right.context.toolCallId &&
        left.context.account == right.context.account &&
        left.context.venue == right.context.venue &&
        left.context.executionDomain == right.context.executionDomain &&
        left.instrument == right.instrument &&
        left.contract.symbol == right.contract.symbol &&
        left.contract.currency == right.contract.currency &&
        left.contract.secType == right.contract.secType &&
        left.order.action == right.order.action &&
        left.order.orderType == right.order.orderType &&
        left.order.totalQuantity == right.order.totalQuantity &&
        left.order.lmtPrice == right.order.lmtPrice &&
        left.timeInForce == right.timeInForce &&
        left.referencePrice == right.referencePrice &&
        left.expiresAtMs == right.expiresAtMs;
}

class FakeAuthority : public ExecutionAuthority,
                      public ExecutionReadAuthority
{
public:
    ExecutionCommandResult PlaceOrder(const PlaceOrderCommand& command) override
    {
        ++placeCalls;
        lastPlace = command;
        ExecutionCommandResult result;
        result.status = ExecutionCommandStatus::Accepted;
        result.commandId = command.context.toolCallId;
        result.orderId = 42;
        return result;
    }

    ExecutionCommandResult CancelOrder(const CancelOrderCommand& command) override
    {
        ExecutionCommandResult result;
        result.status = ExecutionCommandStatus::Accepted;
        result.commandId = command.context.toolCallId;
        result.orderId = command.orderId;
        return result;
    }

    ExecutionCommandResult FlattenPosition(
        const FlattenPositionCommand& command) override
    {
        ++flattenCalls;
        lastFlatten = command;
        ExecutionCommandResult result;
        result.status = ExecutionCommandStatus::Accepted;
        result.commandId = command.context.toolCallId;
        result.orderId = 43;
        return result;
    }

    ExecutionCommandResult PreviewOrder(
        const PlaceOrderCommand& command) override
    {
        ExecutionCommandResult result;
        result.status = ExecutionCommandStatus::Accepted;
        result.commandId = command.context.toolCallId;
        return result;
    }

    ExecutionCommandResult PreviewFlattenPosition(
        const FlattenPositionCommand& command) override
    {
        ExecutionCommandResult result;
        result.status = ExecutionCommandStatus::Accepted;
        result.commandId = command.context.toolCallId;
        result.hasAuthoritativeFlattenSnapshot = true;
        result.authoritativeFlattenPositionQuantity = -250.0;
        result.authoritativeFlattenConnectionEpoch = 7;
        result.authoritativeFlattenPositionGeneration = 11;
        result.authoritativeFlattenPlanBinding =
            "sha256:aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa";
        return result;
    }

    bool IsDurablePlaceReplay(
        const PlaceOrderCommand& command) const override
    {
        return durablePlaceReplay && SamePlacePayload(command, durablePlace);
    }

    bool IsDurableFlattenReplay(
        const FlattenPositionCommand& command) const override
    {
        return durableFlattenReplay &&
            command.context.agentId == durableFlatten.context.agentId &&
            command.context.sessionId == durableFlatten.context.sessionId &&
            command.context.toolCallId == durableFlatten.context.toolCallId &&
            command.instrument == durableFlatten.instrument;
    }

    ExecutionCommandResult ReadAuthoritativeState(
        const ExecutionReadCommand& command) override
    {
        ExecutionCommandResult result;
        result.status = ExecutionCommandStatus::Accepted;
        result.commandId = command.context.toolCallId;
        return result;
    }

    int placeCalls = 0;
    int flattenCalls = 0;
    bool durablePlaceReplay = false;
    bool durableFlattenReplay = false;
    PlaceOrderCommand durablePlace;
    FlattenPositionCommand durableFlatten;
    PlaceOrderCommand lastPlace;
    FlattenPositionCommand lastFlatten;
};

PlaceOrderCommand Place(const std::string& callId)
{
    PlaceOrderCommand command;
    command.context.agentId = "permit-agent";
    command.context.sessionId = "permit-session";
    command.context.toolCallId = callId;
    command.context.account = "SIM";
    command.context.venue = "SIMULATOR";
    command.context.executionDomain = "SIM:permit-agent";
    command.contract.symbol = "EUR";
    command.contract.currency = "USD";
    command.contract.secType = "CASH";
    command.contract.exchange = "IDEALPRO";
    command.order.action = "BUY";
    command.order.orderType = "LMT";
    command.order.totalQuantity = 1000.0;
    command.order.lmtPrice = 1.1002;
    command.instrument = "EUR.USD";
    command.timeInForce = "DAY";
    command.referencePrice = 1.1002;
    command.expiresAtMs = NowMs() + 60000;
    return command;
}

FlattenPositionCommand Flatten(const std::string& callId)
{
    FlattenPositionCommand command;
    command.context.agentId = "permit-agent";
    command.context.sessionId = "permit-session";
    command.context.toolCallId = callId;
    command.context.account = "SIM";
    command.context.venue = "SIMULATOR";
    command.context.executionDomain = "SIM:permit-agent";
    command.contract.symbol = "EUR";
    command.contract.currency = "USD";
    command.contract.secType = "CASH";
    command.contract.exchange = "IDEALPRO";
    command.instrument = "EUR.USD";
    return command;
}
}

class PreviewPermitTestAccess
{
public:
    static bool IssuePlace(UnixExecutionServiceServer& server,
                           const PlaceOrderCommand& command,
                           std::string& permit,
                           std::string& mutationCommandId,
                           long long& expiresAtMs,
                           std::string& reason)
    {
        return server.IssuePreviewPermit(
            command, permit, mutationCommandId, expiresAtMs, reason);
    }

    static bool ConsumePlace(UnixExecutionServiceServer& server,
                             const PlaceOrderCommand& command,
                             std::string& reason)
    {
        return server.ConsumePreviewPermit(command, reason);
    }

    static bool IssueFlatten(UnixExecutionServiceServer& server,
                             const FlattenPositionCommand& command,
                             const ExecutionCommandResult& preview,
                             std::string& permit,
                             std::string& mutationCommandId,
                             long long& expiresAtMs,
                             std::string& reason)
    {
        return server.IssueFlattenPreviewPermit(
            command, preview, permit, mutationCommandId, expiresAtMs, reason);
    }

    static bool ConsumeFlatten(UnixExecutionServiceServer& server,
                               FlattenPositionCommand& command,
                               std::string& reason)
    {
        return server.ConsumeFlattenPreviewPermit(command, reason);
    }

    static void Expire(UnixExecutionServiceServer& server,
                       const std::string& permit)
    {
        std::lock_guard<std::mutex> lock(server.m_previewMutex);
        std::unordered_map<std::string,
            UnixExecutionServiceServer::PreviewPermitRecord>::iterator found =
                server.m_previewPermits.find(permit);
        REQUIRE(found != server.m_previewPermits.end());
        found->second.steadyExpiresAt =
            std::chrono::steady_clock::now() - std::chrono::milliseconds(1);
    }

    static std::size_t Size(UnixExecutionServiceServer& server)
    {
        std::lock_guard<std::mutex> lock(server.m_previewMutex);
        return server.m_previewPermits.size();
    }

    static void RevokeOwner(UnixExecutionServiceServer& server,
                            const std::string& agentId,
                            const std::string& sessionId)
    {
        server.RevokePreviewPermitsForOwner(agentId, sessionId);
    }

    static ExecutionCommandResult DispatchPlace(
        UnixExecutionServiceServer& server,
        const PlaceOrderCommand& command)
    {
        return server.DispatchPlaceOrder(command);
    }

    static ExecutionCommandResult DispatchFlatten(
        UnixExecutionServiceServer& server,
        const FlattenPositionCommand& command)
    {
        return server.DispatchFlattenPosition(command);
    }
};

namespace
{
void TestPlacePermitSingleUseAndAtomicMismatch()
{
    FakeAuthority authority;
    UnixExecutionServiceServer server(authority);

    PlaceOrderCommand preview = Place("preview-one");
    std::string permit;
    std::string mutationId;
    std::string reason;
    long long expiry = 0;
    REQUIRE(PreviewPermitTestAccess::IssuePlace(
        server, preview, permit, mutationId, expiry, reason));
    REQUIRE(!permit.empty());
    REQUIRE(!mutationId.empty());
    REQUIRE(expiry > NowMs());

    PlaceOrderCommand mutation = preview;
    mutation.context.toolCallId = mutationId;
    mutation.previewPermit = permit;
    REQUIRE(PreviewPermitTestAccess::ConsumePlace(server, mutation, reason));
    REQUIRE(!PreviewPermitTestAccess::ConsumePlace(server, mutation, reason));
    REQUIRE(!reason.empty());

    preview = Place("preview-mismatch");
    REQUIRE(PreviewPermitTestAccess::IssuePlace(
        server, preview, permit, mutationId, expiry, reason));
    PlaceOrderCommand wrong = preview;
    wrong.context.toolCallId = mutationId + "-wrong";
    wrong.previewPermit = permit;
    REQUIRE(!PreviewPermitTestAccess::ConsumePlace(server, wrong, reason));
    PlaceOrderCommand formerlyCorrect = preview;
    formerlyCorrect.context.toolCallId = mutationId;
    formerlyCorrect.previewPermit = permit;
    REQUIRE(!PreviewPermitTestAccess::ConsumePlace(
        server, formerlyCorrect, reason));
}

void TestPlacePermitBindsPayloadOwnerExpiryAndRevocation()
{
    FakeAuthority authority;
    UnixExecutionServiceServer server(authority);
    std::string permit;
    std::string mutationId;
    std::string reason;
    long long expiry = 0;

    PlaceOrderCommand preview = Place("preview-payload");
    REQUIRE(PreviewPermitTestAccess::IssuePlace(
        server, preview, permit, mutationId, expiry, reason));
    PlaceOrderCommand changed = preview;
    changed.context.toolCallId = mutationId;
    changed.previewPermit = permit;
    changed.order.totalQuantity += 1.0;
    REQUIRE(!PreviewPermitTestAccess::ConsumePlace(server, changed, reason));

    preview = Place("preview-expired");
    REQUIRE(PreviewPermitTestAccess::IssuePlace(
        server, preview, permit, mutationId, expiry, reason));
    PreviewPermitTestAccess::Expire(server, permit);
    PlaceOrderCommand expired = preview;
    expired.context.toolCallId = mutationId;
    expired.previewPermit = permit;
    REQUIRE(!PreviewPermitTestAccess::ConsumePlace(server, expired, reason));

    preview = Place("preview-revoke");
    REQUIRE(PreviewPermitTestAccess::IssuePlace(
        server, preview, permit, mutationId, expiry, reason));
    REQUIRE(PreviewPermitTestAccess::Size(server) == 1);
    PreviewPermitTestAccess::RevokeOwner(
        server, preview.context.agentId, preview.context.sessionId);
    REQUIRE(PreviewPermitTestAccess::Size(server) == 0);
    PlaceOrderCommand revoked = preview;
    revoked.context.toolCallId = mutationId;
    revoked.previewPermit = permit;
    REQUIRE(!PreviewPermitTestAccess::ConsumePlace(server, revoked, reason));
}

void TestFlattenPermitInjectsExactAuthoritativeGeneration()
{
    FakeAuthority authority;
    UnixExecutionServiceServer server(authority);
    FlattenPositionCommand previewCommand = Flatten("flatten-preview");
    const ExecutionCommandResult preview =
        authority.PreviewFlattenPosition(previewCommand);
    std::string permit;
    std::string mutationId;
    std::string reason;
    long long expiry = 0;
    REQUIRE(PreviewPermitTestAccess::IssueFlatten(
        server, previewCommand, preview, permit, mutationId, expiry, reason));

    FlattenPositionCommand mutation = previewCommand;
    mutation.context.toolCallId = mutationId;
    mutation.previewPermit = permit;
    REQUIRE(PreviewPermitTestAccess::ConsumeFlatten(server, mutation, reason));
    REQUIRE(mutation.previewPermit.empty());
    REQUIRE(mutation.hasAuthoritativePreviewSnapshot);
    REQUIRE(mutation.previewPositionQuantity == -250.0);
    REQUIRE(mutation.previewPositionConnectionEpoch == 7);
    REQUIRE(mutation.previewPositionGeneration == 11);
    REQUIRE(mutation.authoritativePreviewPlanBinding ==
        preview.authoritativeFlattenPlanBinding);
    REQUIRE(!PreviewPermitTestAccess::ConsumeFlatten(server, mutation, reason));
}

void TestDurableReplayBypassesPermitOnlyForExactPayload()
{
    FakeAuthority authority;
    UnixExecutionServiceServer server(authority);
    PlaceOrderCommand durable = Place("durable-place-command");
    durable.previewPermit.clear();
    authority.durablePlace = durable;
    authority.durablePlaceReplay = true;

    ExecutionCommandResult result =
        PreviewPermitTestAccess::DispatchPlace(server, durable);
    REQUIRE(result.status == ExecutionCommandStatus::Accepted);
    REQUIRE(authority.placeCalls == 1);

    PlaceOrderCommand changed = durable;
    changed.order.totalQuantity += 5.0;
    result = PreviewPermitTestAccess::DispatchPlace(server, changed);
    REQUIRE(result.status == ExecutionCommandStatus::Rejected);
    REQUIRE(authority.placeCalls == 1);
    REQUIRE(!result.reasonCode.empty());

    FlattenPositionCommand durableFlatten = Flatten("durable-flatten-command");
    durableFlatten.previewPermit.clear();
    authority.durableFlatten = durableFlatten;
    authority.durableFlattenReplay = true;
    result = PreviewPermitTestAccess::DispatchFlatten(server, durableFlatten);
    REQUIRE(result.status == ExecutionCommandStatus::Accepted);
    REQUIRE(authority.flattenCalls == 1);

    FlattenPositionCommand changedFlatten = durableFlatten;
    changedFlatten.instrument = "GBP.USD";
    result = PreviewPermitTestAccess::DispatchFlatten(server, changedFlatten);
    REQUIRE(result.status == ExecutionCommandStatus::Rejected);
    REQUIRE(authority.flattenCalls == 1);
}
}

int main()
{
    TestPlacePermitSingleUseAndAtomicMismatch();
    TestPlacePermitBindsPayloadOwnerExpiryAndRevocation();
    TestFlattenPermitInjectsExactAuthoritativeGeneration();
    TestDurableReplayBypassesPermitOnlyForExactPayload();
    return 0;
}
'''
          write("tests/execution_preview_permit_tests.cpp", permit_test)

          telemetry_test = r'''#include "../HeptaTrade/observability/runtime_telemetry.h"

#include <chrono>
#include <cstdlib>
#include <iostream>
#include <string>
#include <thread>

namespace
{
void Require(bool condition, const char* expression, int line)
{
    if (condition) return;
    std::cerr << "requirement failed at line " << line << ": "
              << expression << '\n';
    std::abort();
}
#define REQUIRE(expression) Require(static_cast<bool>(expression), #expression, __LINE__)
}

int main()
{
    RuntimeTelemetry& telemetry = RuntimeTelemetry::Global();
    telemetry.ResetForTests();
    RuntimeRecordRiskDecision(true, "RISK_OK");
    RuntimeRecordRiskDecision(false, "RISK_ORDER_NOTIONAL_LIMIT");
    RuntimeRecordToolOutcome("intent.apply_target_position", 0, "none");
    RuntimeRecordToolOutcome("trade.place_order", 1, "CAPABILITY_REQUIRED");
    RuntimeRecordJournalEvent("place_send_attempt", "attempt", "none");
    RuntimeRecordJournalEvent(
        "execution_projection_failed", "uncertain", "STATE_BREAK");
    RuntimeRecordJournalFailure("OMS_FDATASYNC_FAILED");
    RuntimeRecordReconcile("startup", "complete", "none");
    RuntimeRecordKillSwitch("blocked");
    telemetry.SetGaugeKey("hepta_active_orders", 3.0);
    {
        RuntimeLatencyScope scope(
            "hepta_snapshot_risk_latency_microseconds", "operation", "evaluate");
        std::this_thread::sleep_for(std::chrono::milliseconds(1));
    }
    const std::string sensitive =
        "account DU123 credential secret with spaces that must never be a label";
    RuntimeRecordToolOutcome(sensitive, 3, sensitive);

    const std::string snapshot = telemetry.SnapshotJson();
    REQUIRE(snapshot.find("hepta_risk_decisions_total") != std::string::npos);
    REQUIRE(snapshot.find("hepta_tool_calls_total") != std::string::npos);
    REQUIRE(snapshot.find("hepta_session_rejections_total") != std::string::npos);
    REQUIRE(snapshot.find("hepta_execution_commands_total") != std::string::npos);
    REQUIRE(snapshot.find("hepta_oms_journal_failures_total") != std::string::npos);
    REQUIRE(snapshot.find("hepta_venue_sends_total") != std::string::npos);
    REQUIRE(snapshot.find("hepta_execution_events_total") != std::string::npos);
    REQUIRE(snapshot.find("hepta_reconcile_runs_total") != std::string::npos);
    REQUIRE(snapshot.find("hepta_state_breaks_total") != std::string::npos);
    REQUIRE(snapshot.find("hepta_kill_switch_transitions_total") != std::string::npos);
    REQUIRE(snapshot.find("hepta_snapshot_risk_latency_microseconds") !=
            std::string::npos);
    REQUIRE(snapshot.find("credential secret") == std::string::npos);

    for (int i = 0; i < 5000; ++i)
        telemetry.IncrementKey(
            "hepta_bounded_series_test_total", "series=" + std::to_string(i));
    REQUIRE(telemetry.SeriesCount() <= 2048);
    REQUIRE(telemetry.SnapshotJson().find("\"dropped_series\":0") ==
            std::string::npos);
    return 0;
}
'''
          write("tests/runtime_telemetry_tests.cpp", telemetry_test)

          tests_cmake = read("tests/CMakeLists.txt")
          test_block = '''
add_executable(hepta_execution_preview_permit_tests
    execution_preview_permit_tests.cpp)
target_link_libraries(hepta_execution_preview_permit_tests
    hepta_execution_core)
hepta_register_core_test(hepta_execution_preview_permit_tests)

add_executable(hepta_runtime_telemetry_tests
    runtime_telemetry_tests.cpp)
target_link_libraries(hepta_runtime_telemetry_tests
    hepta_observability_core)
hepta_register_core_test(hepta_runtime_telemetry_tests)

'''
          if "hepta_execution_preview_permit_tests" not in tests_cmake:
              marker = "add_custom_target(hepta_core_test_binaries\n"
              if marker not in tests_cmake:
                  raise RuntimeError("tests/CMakeLists.txt: custom target marker missing")
              tests_cmake = tests_cmake.replace(marker, test_block + marker, 1)
              write("tests/CMakeLists.txt", tests_cmake)

          # Temporary write/export workflows must not survive the reviewed tree.
          for relative in (
              ".github/workflows/dev-gap-closure-run.yml",
              ".github/workflows/dev-source-export.yml",
          ):
              path = ROOT / relative
              if path.exists():
                  path.unlink()
          PY

      - name: Verify exact resulting tree
        env:
          HEPTA_CMAKE_GENERATOR: Ninja
          HEPTA_JOBS: "2"
        run: |
          python3 scripts/check_repository_integrity.py
          ./scripts/dev_core.sh
          stage="$RUNNER_TEMP/heptatrader-runtime"
          DESTDIR="$stage" cmake --install build/core-release --component runtime
          test -x "$stage/usr/local/libexec/heptatrader/hepta-executiond"

      - name: Commit verified closure slice
        run: |
          git config user.name "Tomasrgbsf"
          git config user.email "tomasbraynt@gmail.com"
          git add -A
          git diff --cached --check
          git commit -s -m "feat: close permit lifecycle and runtime telemetry gaps"
          git push origin HEAD:codex/heptatrader-gap-closure-v2

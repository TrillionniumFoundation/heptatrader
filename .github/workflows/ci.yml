name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: heptatrader-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  repository-contracts:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Repository contracts
        run: python3 scripts/check_repo_contracts.py
      - name: Python safety tests
        run: python3 -m unittest discover -s tests/python -p 'test_*.py' -v

  core:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        compiler: [gcc, clang]
        build_type: [Debug, Release]
    env:
      CC: ${{ matrix.compiler == 'gcc' && 'gcc' || 'clang' }}
      CXX: ${{ matrix.compiler == 'gcc' && 'g++' || 'clang++' }}
    steps:
      - uses: actions/checkout@v4
      - name: Install build dependencies
        run: sudo apt-get update && sudo apt-get install -y ninja-build libssl-dev
      - name: Configure
        run: >-
          cmake -S . -B build -G Ninja
          -DCMAKE_BUILD_TYPE=${{ matrix.build_type }}
          -DBUILD_TESTING=ON
          -DHEPTA_ENABLE_IBAPI=OFF
          -DHEPTA_ENABLE_LEGACY_0DTE_BRIDGE=OFF
          -DHEPTA_BUILD_LEGACY_MONOLITH=OFF
          -DHEPTA_BUILD_LEGACY_SIMULATOR=OFF
          -DHEPTA_ENABLE_HARDENING=ON
      - name: Build core runtime and tests
        run: >-
          cmake --build build --parallel 2 --target
          hepta_core_test_binaries
          hepta_tool_gatewayd
          hepta_executiond
          heptactl
          hepta_sessionctl
      - name: Test
        run: ctest --test-dir build --output-on-failure --parallel 2 -L core

  asan-ubsan:
    runs-on: ubuntu-latest
    env:
      ASAN_OPTIONS: detect_leaks=1:halt_on_error=1:strict_string_checks=1
      UBSAN_OPTIONS: halt_on_error=1:print_stacktrace=1
    steps:
      - uses: actions/checkout@v4
      - name: Install build dependencies
        run: sudo apt-get update && sudo apt-get install -y ninja-build libssl-dev
      - name: Configure sanitizers
        run: >-
          cmake -S . -B build -G Ninja
          -DCMAKE_BUILD_TYPE=Debug
          -DBUILD_TESTING=ON
          -DHEPTA_ENABLE_IBAPI=OFF
          -DHEPTA_ENABLE_LEGACY_0DTE_BRIDGE=OFF
          -DHEPTA_BUILD_LEGACY_MONOLITH=OFF
          -DHEPTA_BUILD_LEGACY_SIMULATOR=OFF
          -DHEPTA_ENABLE_ASAN_UBSAN=ON
      - name: Build
        run: cmake --build build --parallel 2 --target hepta_core_test_binaries
      - name: Test
        run: ctest --test-dir build --output-on-failure --parallel 1 -L core

  package:
    runs-on: ubuntu-latest
    needs: [repository-contracts, core]
    steps:
      - uses: actions/checkout@v4
      - name: Install build dependencies
        run: sudo apt-get update && sudo apt-get install -y ninja-build libssl-dev
      - name: Configure release
        run: >-
          cmake -S . -B build -G Ninja
          -DCMAKE_BUILD_TYPE=Release
          -DBUILD_TESTING=ON
          -DHEPTA_ENABLE_IBAPI=OFF
          -DHEPTA_ENABLE_LEGACY_0DTE_BRIDGE=OFF
          -DHEPTA_BUILD_LEGACY_MONOLITH=OFF
          -DHEPTA_BUILD_LEGACY_SIMULATOR=OFF
          -DHEPTA_ENABLE_HARDENING=ON
      - name: Build runtime
        run: cmake --build build --parallel 2
      - name: Stage and validate install tree
        run: |
          cmake --install build --prefix "$RUNNER_TEMP/stage/usr"
          python3 scripts/verify_install_tree.py \
            --root "$RUNNER_TEMP/stage/usr" \
            --manifest "$RUNNER_TEMP/stage/install-manifest.json"
          python3 scripts/generate_sbom.py \
            --root "$RUNNER_TEMP/stage/usr" \
            --version-file VERSION \
            --git-sha "$GITHUB_SHA" \
            --output "$RUNNER_TEMP/stage/heptatrader.spdx.json"
      - name: Build package
        run: |
          mkdir -p dist
          cpack --config build/CPackConfig.cmake -B dist
          cp "$RUNNER_TEMP/stage/install-manifest.json" dist/
          cp "$RUNNER_TEMP/stage/heptatrader.spdx.json" dist/
          (cd dist && sha256sum * > SHA256SUMS)
      - uses: actions/upload-artifact@v4
        with:
          name: heptatrader-${{ github.sha }}
          path: dist/
          if-no-files-found: error
          retention-days: 14
